{
  "skill_name": "gen-task-in-plan",
  "evals": [
    {
      "id": 0,
      "name": "compatible-append",
      "prompt": "在 fixtures/pxbox-26324_plan.md 這份 plan 加一個 task：把 OrderQuantity.cs 測試檔裡 [Trait(\"PromotionCondition\", \"OrderPrice\")] 這個 typo 改成 \"OrderQuantity\"（這個 Trait 標籤標錯案型了）。",
      "expected_output": "skill 判定為 compatible（純 typo 修正、不改 prod 行為）。不直接修改原 plan 既有章節；建立獨立衍生檔 pxbox-26324_ft_01.md（事先詢問或預設 {XXX}=01 標籤），檔首引用原 plan；在衍生檔內寫入 FT01 task detail block，欄位順序：Current state → Goal → Dependency → Target → Implementation Details → Affected Files；衍生檔附 Follow-up Task 進度表並新增 FT01 列 Status=Todo；原 plan 末新增 ## Follow-up Task 章節引用衍生檔。回報新 task ID 與衍生檔路徑。",
      "files": ["fixtures/pxbox-26324_plan.md"],
      "expectations": [
        "原始 fixture (evals/fixtures/pxbox-26324_plan.md) 內容未被修改",
        "outputs 目錄含 plan 副本，且 plan 副本新增了 `## Follow-up Task` 章節",
        "`## Follow-up Task` 章節引用 `pxbox-26324_ft_*.md` 衍生檔",
        "outputs 目錄含 `pxbox-26324_ft_01.md`（或對應的 ft_{XXX} 衍生檔）",
        "ft 衍生檔含 `FT01` task block",
        "FT01 task block 含全部 6 個必要欄位：Current state / Goal / Dependency / Target / Implementation Details / Affected Files",
        "ft 衍生檔含 `Follow-up Task 進度表`，且有 FT01 列、Status=Todo",
        "ft 衍生檔檔首引用原 plan（pxbox-26324_plan.md）",
        "plan 副本中既有的 `## Design` 與 `## Task` 章節未被修改"
      ]
    },
    {
      "id": 1,
      "name": "violating-stop",
      "prompt": "在 fixtures/pxbox-26325_plan.md 加一個 task：在 CalculateDiscountService 新增一個「滿額免運」的折扣案型，包含 prod 邏輯跟測試。",
      "expected_output": "skill 判定為 violating（plan Req 是『測試重組、零行為變更、零生產碼動到』，但新 task 要動 prod + 加新案型）。STOP 不寫任何檔（不建立 _ft_*.md，也不改原 plan），引用被違反的 Req 段落，提兩個選項（開新 plan / 改原 Req），等 user 決定。",
      "files": ["fixtures/pxbox-26325_plan.md"],
      "expectations": [
        "原始 fixture (evals/fixtures/pxbox-26325_plan.md) 內容未被修改",
        "outputs 目錄沒有任何 `_ft_*.md` 衍生檔",
        "outputs 目錄沒有對 plan 副本做任何「加 task」的修改（沒有新增 ## Follow-up Task、沒有在 Task 章節新增條目）",
        "outputs 含 response.md 或等效的回覆，引述被違反的 Req / 約束（測試重組 / 零生產碼動到 / R04 之一）",
        "回覆提供兩個選項（開新 plan / 修改原 Req 二者皆有提及）"
      ]
    },
    {
      "id": 2,
      "name": "ambiguous-surface",
      "prompt": "在 fixtures/pxbox-26326_plan.md 加一個 task：對 CheckPromoteCondition 補一個測試，覆蓋傳入 null promotion 的邊界情況。",
      "expected_output": "skill 判定為 ambiguous（補測試本身 compatible，但 null case 是否曝出 prod 未處理、迫使後續修 prod 取決於 prod 現況）。不寫任何檔，列出兩種詮釋與各自假設，請 user 判斷。",
      "files": ["fixtures/pxbox-26326_plan.md"],
      "expectations": [
        "原始 fixture (evals/fixtures/pxbox-26326_plan.md) 內容未被修改",
        "outputs 目錄沒有任何 `_ft_*.md` 衍生檔",
        "outputs 目錄沒有對 plan 副本做任何「加 task」的修改（沒有新增 ## Follow-up Task、沒有在 Task 章節新增條目）",
        "outputs 含 response.md 或等效的回覆，呈現至少兩種可能詮釋",
        "回覆說明每種詮釋依賴的假設（prod 是否處理 null）"
      ]
    }
  ]
}
